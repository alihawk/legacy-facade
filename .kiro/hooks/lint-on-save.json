{
  "name": "lint-on-save",
  "description": "Auto-lint and format frontend TypeScript and backend Python on save",
  "enabled": true,
  "triggers": [
    {
      "type": "onFileSave",
      "patterns": [
        "frontend/src/**/*.{ts,tsx}",
        "backend/**/*.py"
      ]
    }
  ],
  "actions": [
    {
      "type": "command",
      "cwd": "frontend",
      "command": "npm",
      "args": ["run", "lint", "--", "--fix"],
      "description": "Fix ESLint issues in frontend TypeScript files"
    }
  ]
}
